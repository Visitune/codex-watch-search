# Vercel Postgres (Neon) — copy to .env.local
DATABASE_URL=postgresql://user:password@host/db?sslmode=require
# Vercel Blob
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxx
# Cron protection (optional)
CRON_SECRET=change-me
